"""The browserless attacker CLI.

Every attack keyjack demonstrates is reproducible with no browser at all — the client was
never a boundary. This package re-implements only what the served client already publishes.
"""
