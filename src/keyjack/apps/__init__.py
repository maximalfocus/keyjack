"""Application entry points.

Each application variant is a separate, explicitly named entry point so a vulnerable
surface can never be reached by default. This baseline ships the secure application only.
"""
